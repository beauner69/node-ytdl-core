// type MaybeString = null | string;

import { DeependArtifact } from '@zerolabs/deepend';

export type FactsFound = {
  // MonorepoRoot: string;
  // TempRoot: string;
  // BugsnagKey: string;
  // PistolVersion: string;
  // AndroidBinaryVersion: string;
  // IosBinaryVersion: string;
};

export type BillOfWorks = {
  StagingArea: string;
  // ios_wrath: boolean;
  // android_wrath: boolean;
  // web_wrath: boolean;
  // web_hawkeye: boolean;

  // // Whether we should actually run this fucker
  // actually_do_it: boolean;
};
